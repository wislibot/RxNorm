You are TRAE SOLO CODE working in the repo:
  e:/TRAE/Projects/RxNorm/

Goal (do in this order):
1) Reduce Supabase DB size safely (keep only curated + essential raw), without breaking the app/ETL.
2) Investigate and fix the QC metric `all1_overlap_pct` showing 100% (likely wrong).
3) Don’t implement broader mismatch-loosening yet, BUT keep “Option A” ready as a follow-up: hydrate/salt equivalence as an OPTIONAL/FLAGGED normalization step.

Constraints:
- Prioritize correctness + reproducibility. Avoid irreversible drops until we have backups/exports.
- Prefer: curated tables as the source of truth for the app; raw tables only for rebuild/debug.
- Keep changes small and reviewable; add short docs for operators.

--------------------------------------------------------------------
1) SUPABASE SIZE CLEANUP (SAFE PROCEDURE)
--------------------------------------------------------------------

1.1 Inspect what is large (SQL in Supabase)
Run these queries and paste results back:

-- Biggest tables by total size
SELECT
  schemaname,
  relname AS table_name,
  pg_size_pretty(pg_total_relation_size(quote_ident(schemaname)||'.'||quote_ident(relname))) AS total_size,
  pg_total_relation_size(quote_ident(schemaname)||'.'||quote_ident(relname)) AS total_bytes
FROM pg_stat_user_tables
ORDER BY total_bytes DESC
LIMIT 30;

-- Row counts (approx) for key raw tables (adjust names if different)
SELECT 'raw_nhi_drug_items' AS t, count(*) FROM raw_nhi_drug_items
UNION ALL SELECT 'raw_tfda_permits', count(*) FROM raw_tfda_permits
UNION ALL SELECT 'raw_all1_price_history', count(*) FROM raw_all1_price_history
UNION ALL SELECT 'rx_drug_products', count(*) FROM rx_drug_products
UNION ALL SELECT 'rx_ingredient_concepts', count(*) FROM rx_ingredient_concepts
UNION ALL SELECT 'rx_product_ingredients', count(*) FROM rx_product_ingredients
UNION ALL SELECT 'rx_review_queue', count(*) FROM rx_review_queue;

1.2 Decide retention tiers (default recommendation)
Keep in DB:
- Curated tables (rx_*): required by the app and QC
- Minimal raw tables needed to rebuild curated (raw_nhi, raw_tfda, raw_atc_latest)
Move out / drop from DB once exported:
- raw_all1_* (price/effective-date history) IF it’s large and not used by the app queries
- Any intermediate staging tables created during ETL

1.3 Export BEFORE dropping
If a raw table must be retained for audit, export to:
- Supabase Storage (CSV/Parquet), OR
- local artifact kept in the repo’s data/ directory or operator machine.

Provide an operator script or doc to recreate raw tables from source files, so we can drop safely.

1.4 Implement “rebuild-from-files” reliability
In the ETL, ensure the canonical rebuild path is:
source files (.csv/.gz) -> raw_* (optional) -> curated rx_*
If raw_* is absent, ETL should be able to recreate it (idempotent).

Deliverables for this section:
- A short markdown doc: `docs/supabase_storage_policy.md`
  - list which tables are required, which are optional, and how to rebuild them.
- A small “operator checklist” section: export -> verify export -> drop -> vacuum/analyze.

--------------------------------------------------------------------
2) FIX / DEBUG `all1_overlap_pct = 100%`
--------------------------------------------------------------------

Working hypothesis:
- The overlap is being computed against the wrong dataset (e.g., comparing NHI to itself),
  OR raw_all1 import got polluted with NHI codes, OR the metric is using an incorrect join key.

2.1 Establish the truth with direct set counts (SQL)
Run these (adjust table/column names to actual schema):

-- A) How many distinct NHI product codes exist?
SELECT count(DISTINCT nhi_code) AS nhi_codes
FROM rx_drug_products;

-- B) How many distinct codes exist in all1 history?
-- Replace `drug_code` with the actual all1 code column.
SELECT count(DISTINCT drug_code) AS all1_codes
FROM raw_all1_price_history;

-- C) Overlap size
SELECT count(DISTINCT p.nhi_code) AS overlap_codes
FROM rx_drug_products p
JOIN raw_all1_price_history a
  ON a.drug_code = p.nhi_code;

-- D) Expected: overlap_codes <= all1_codes and overlap_codes <= nhi_codes

2.2 If overlap is still 100%, find what’s wrong
- Compare (nhi_codes - overlap_codes) should be > 0 if all1 is a subset.
- If nhi_codes == overlap_codes:
  - Check if raw_all1_price_history accidentally contains all NHI codes:
    - Inspect sample non-overlapping codes from earlier analysis if available.
  - Check if drug_code is being normalized/padded incorrectly (e.g., all codes become same length and match).

2.3 Patch qc_report logic
Locate the QC computation in:
- e:/TRAE/Projects/RxNorm/etl/qc_report.py (or report_qc.py per repo structure)
Find where `all1_overlap_pct` is computed and ensure:
- It compares DISTINCT code sets between the correct tables
- It uses the correct code columns
- It does not accidentally join rx_drug_products to raw_nhi instead of raw_all1
- It does not compute overlap as (overlap / overlap) by using the same subquery twice

Add explicit QC debug fields:
- all1_distinct_codes
- overlap_distinct_codes
- overlap_pct = overlap_distinct_codes::float / NULLIF(nhi_distinct_codes, 0)

Add a tiny regression test or at least a deterministic “QC sanity check”:
- assert overlap_pct <= 1.0
- assert all1_distinct_codes <= nhi_distinct_codes (if that’s empirically true for this dataset)

Deliverables for this section:
- Code fix to QC metric + updated qc_report output
- A short note in docs describing what `all1_overlap_pct` means and why it’s expected to be < 100%

--------------------------------------------------------------------
3) OPTION A (DO NOT FORGET): HYDRATE/SALT EQUIVALENCE AS A FLAG
--------------------------------------------------------------------

After sections (1) and (2) are done and verified, prepare Option A as a follow-up PR:

Goal:
- Reduce remaining `tfda_mismatch` noise by treating hydrates and common salt forms as equivalent,
  but ONLY when enabled (feature flag / config), so we can keep “strict mode” as default.

Requirements:
- Add a config flag, e.g.:
  - STRICT_COMPARE (default true)
  - EQUIV_HYDRATES (default false)
  - EQUIV_SALTS (default false)
- Implement equivalence in compare-token generation (not in canonical stored names):
  - Normalize hydrate tokens: MONOHYDRATE, DIHYDRATE, TRIHYDRATE, HEMIHYDRATE, ANHYDROUS, XH2O patterns
  - Normalize common salts: HCL/HYDROCHLORIDE, SODIUM, POTASSIUM, CALCIUM, MESYLATE, BESYLATE, TARTRATE, etc.
- Produce QC deltas:
  - pending count before/after
  - examples of mismatches that become matches under Option A
  - ensure we’re not hiding true multi-ingredient mismatches

Acceptance criteria:
- In strict mode: behavior unchanged (same mismatch count as current baseline)
- With flags on: mismatch queue decreases measurably and explainably

